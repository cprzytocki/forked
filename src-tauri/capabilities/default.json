{
  "$schema": "https://schemas.tauri.app/next/schema/capability",
  "identifier": "default",
  "description": "Default capabilities for the Git Client",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message",
    "dialog:allow-ask",
    "dialog:allow-confirm"
  ]
}
